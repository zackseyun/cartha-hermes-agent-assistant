{
  "name": "cartha-hermes-local",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "dev": "HERMES_UI_PORT=5128 node --watch server.mjs",
    "check": "node --check server.mjs && node --check public/app.js && node --check scripts/smoke.mjs",
    "smoke": "node scripts/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
